CREATE TABLE "PCR_TEST_POSITIVE"(
state VARCHAR NOT NULL,
date DATE NOT NULL,
new_results_reported INT NOT NULL,
total_results_reported INT NOT NULL,
geocoded_state VARCHAR NOT NUll	
)

SELECT* FROM PCR_TEST_POSITIVE;